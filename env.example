# Environment Configuration for NIYYA Server
# Copy this file to .env and update the values for your environment

# Domain Configuration (REQUIRED for HTTPS)
DOMAIN=yourdomain.com
# Use your actual domain name here (e.g., myapp.example.com)

# Server Configuration
SERVER_IP=yourdomain.com
# For production with HTTPS, use your domain name
# For local development, use localhost

# API Configuration
API_TOKEN=your_secure_api_token_here
# Generate a secure random token for API authentication

# WebSocket Configuration
WEBSOCKET_HOST=yourdomain.com
# For HTTPS: use your domain name
# For local development: use localhost

WEBSOCKET_PORT=443
# For HTTPS: use 443 (standard HTTPS port)
# For local development: use 8765

CLIENT_ID_PREFIX=flutter_user

# Database Configuration
TAILSCALE_IP=127.0.0.1
# If using Tailscale, replace with your Tailscale IP
# Otherwise, keep as 127.0.0.1

# SSL/TLS Configuration
# These are automatically handled by HAProxy and certbot
# No additional configuration needed

